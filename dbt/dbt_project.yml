name: customer360
version: '1.0.0'
config-version: 2

profile: customer360

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  customer360:
    +schema: staging
    +materialized: view

    intermediate:
      +schema: intermediate

    marts:
      +schema: marts
      +materialized: table
